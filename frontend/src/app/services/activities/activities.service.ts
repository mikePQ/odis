import {Injectable} from '@angular/core';
import {Host} from '../../domain/host';

@Injectable()
export class ActivitiesService {

  constructor() { }

  getHostActivity(host: Host) {
    return undefined;
  }
}
